/**
 * Shared utilities for MechJeb maneuver operations
 */
import type { KosConnection } from '../../transport/kos-connection.js';
export interface ManeuverResult {
    success: boolean;
    deltaV?: number;
    timeToNode?: number;
    error?: string;
}
/**
 * Parse a numeric value from kOS output
 * Looks for patterns like "23.80  m/s" or just bare numbers
 */
export declare function parseNumber(output: string): number;
/**
 * Parse time string like "31m 10s" or "5h 23m 10s" to seconds
 */
export declare function parseTimeString(output: string): number;
export declare function delay(ms: number): Promise<void>;
/**
 * Query a numeric value from MechJeb (e.g., "23.80  m/s")
 */
export declare function queryNumber(conn: KosConnection, suffix: string): Promise<number>;
/**
 * Query a numeric value with retry - useful after node creation
 * Retries until non-zero value or max attempts reached
 */
export declare function queryNumberWithRetry(conn: KosConnection, suffix: string, maxAttempts?: number, retryDelayMs?: number): Promise<number>;
/**
 * Query maneuver node info using kOS native commands
 * Returns deltaV and timeToNode for the next maneuver node
 *
 * Uses kOS's NEXTNODE instead of MechJeb INFO suffixes which return "N/A"
 */
export declare function queryNodeInfo(conn: KosConnection): Promise<{
    deltaV: number;
    timeToNode: number;
}>;
/**
 * Query a time value from MechJeb (e.g., "31m 10s")
 */
export declare function queryTime(conn: KosConnection, suffix: string): Promise<number>;
/**
 * Execute a maneuver planning command and return the result with node info
 */
export declare function executeManeuverCommand(conn: KosConnection, cmd: string, timeout?: number): Promise<ManeuverResult>;
//# sourceMappingURL=shared.d.ts.map